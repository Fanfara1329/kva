n = int(input())
if n == 1:
    print(0)
    exit(0)
v = sorted(list(map(int, input().split())))
ans = v[-1] * n
for j in v:
    cur = 0
    for i in range(n):
        if v[i] <= j:
            cur += j - v[i]
        else:
            cur += v[-1] - v[i]
    ans = min(ans, cur)
print(ans)
